# DSH 规则引擎：设计与本轮讨论全记录

- 时间：2026-09-22
- 定位：**只服务 DSH 的库**，不追求通用性（哥哥 2026-09-22 明确）
- 消费者：**context_care** —— 「通知模型」和「篡改上下文」这两类效果归它管
- 执行：请求层的改动目前只能由 **fetch-router** 做（它是唯一拦 `globalThis.fetch` 的插件）。
  这是技术现实，不是它的架构角色：引擎里没有「执行者」这个概念
- 状态：**已实现**（2026-09-22，库能跑、测试全绿、未接入任何插件）——实现记录见 [2026-09-22-规则引擎实现进度.md](2026-09-22-规则引擎实现进度.md)
- 参考实现：
  - SillyTavern 正则扩展：`D:\dev\参考项目\SillyTavern\public\scripts\extensions\regex\`（engine.js 16654 字节 / index.js 87642 字节）
  - s-forge 的 chatseqtrie：`D:\dev\s-forge\packages\chatseqtrie\`（Go）
  - fetch-router 的规则模型：`D:\dev\dsh-fetch-router\lib\rules.js`（11517 字节）

---

## 一、为什么是独立库

同一套规则要被两个插件消费：

- **context_care**：管理者。能提醒的就提醒（`agent.inject(notice(...))`）；提醒不了的，决定改上下文
- **请求层**：改动必须在请求发出去之前那一刻做。目前只有 fetch-router 拦着 `globalThis.fetch`，
  所以由它执行 —— 换一个插件能做到，就换它做，引擎这边什么都不用改

所以规则本身必须是独立库：**核心是纯函数**，输入「规则集 + 事件/上下文」，输出「该做什么」，不依赖 DSH 也不依赖任何具体插件。chatseqtrie 的注释里那句「本文件只提供格式无关的纯逻辑（map/string），不依赖任何具体 SDK」就是这个意思。

---

## 二、规则的完整形状（讨论定稿）

```js
{
  id: 'scrub-empty-reasoning',
  when: { ... },                      // 触发条件
  action: { kind: 'transform', ... }, // 动作
  placement: ['reasoning'],           // 作用位置
  budget: { maxCacheLoss: 0.1 },      // 缓存失效预算（比例）
  cooldownMinutes: 30,                // 冷却（提醒类用）
  oncePerSurface: true,               // 上下文去重（提醒类用）
  order: 10,                          // 顺序
}
```

### when：触发条件

**从酒馆照搬的**

- 完整正则，用 `/pattern/flags` **字符串**形式传（不传 RegExp 对象 —— 跨边界要能序列化），global/sticky 都支持
- 替换语法 `$1` / `$<name>` / `{{match}}`（等价 `$0`）
- `trimStrings`：先匹配一大块，再去掉里面不要的串
- 宏替换：`{{user}}` 这类在替换结果里也能用（酒馆走 substituteParams / substituteParamsExtended）
- `placement`：声明对哪些位置生效（用户输入 / 助手输出 / 思考 / 工具调用 / 工具结果）
- `depth`：只对最近 N 条生效
- `markdownOnly` / `promptOnly`：只改显示，还是只改发出去的
- `runOnEdit`
- 作用域三层 + 白名单（GLOBAL / SCOPED / PRESET）—— 酒馆的注释：`// ORDER MATTERS: defines the regex script priority`，GLOBAL=0 / SCOPED=1 / PRESET=2
- 优先级 = 类型优先级 + 列表内顺序
- 正则编译缓存（LRU，酒馆是 1000 条），取用时重置 `lastIndex`

**DSH 这边要加的**

- `provider` / `model` / `purpose` 维度（fetch-router 已经在匹配了；而且思考内容的形态是跟模型绑定的）
- `args`：工具参数匹配，参数名 → 子串，**数组表示「都要包含」**（例：`{ tool: 'read', args: { path: ['D:/dev/src', '.js'] } }`）
- `idle`：一段时间没调用某个工具（例：`{ idle: { since: 'session_blocks_remember', minutes: 10 } }`）
- `said` / `produced`：文本出现在用户输入或助手输出里

**全部 AND，OR 靠写两条规则**（不做通用表达式引擎）。

### action：动作

两种：

- `notify` —— 提醒。context_care 收到后给模型注入提示；别的插件也可以听同一条规则去做别的事
  （循环输出时它提醒模型，另一个插件提醒用户）
- `transform` —— 修正。context_care 收到后决定做不做，再由能碰请求层的插件去改发出去的请求

`action.by` 指定消费者名，**找不到就跳过而不报错**。不写 `by` 就按 kind 广播给所有订阅者 ——
同一条规则可以多方关心，写两条重复的 when 条件反而是坑。

### 缓存预算（DSH 特有，最新也最核心）

- **字节级前缀估算**：跟最近一次**同接口**（`host + path`）请求的文本比公共前缀。**不建树、不做投影、不做字段策略** —— 比较对象只有上一次，因为缓存也只跟上一次比。
- **预算是第一性的**：规则给「最多损失多少缓存」，**作用范围是算出来的**，不是写死的。
- **预算驱动收缩**：从尾部往前贪心，改一处算一次代价，累计到上限就停。越靠后失效越小，尾部优先是最优解。
- **超预算分两种**：一般是「做不做由人定」（批准）；**隐私类是「做了才能发」（拒绝发送）**。
- **隐私清洗是这套机制的另一端**：预算该给到不惜代价；而字节级匹配在这里正好够用 —— 隐私是确定字符串（key、路径、邮箱、身份证号），上语义判断反而会漏，漏一个等于全漏。

### 生命周期

- 规则来源两种：静态配置（像 fetch-router 现在的 `routes`）+ **运行时由别的插件提供**
- 消费者启动时声明「我消费哪种动作」，引擎按 `by` 找；没写 `by` 就广播给所有订阅该 kind 的
- 冷却（时间）和 surface 去重（当前上下文里有没有过）是**两回事**，分开表达
- **没人接的规则不生效**：指名了但找不到、或者没指名而没人订阅这种动作，规则不执行，
  只产出一条 `no-consumer` 失效事件。篡改没有执行者时请求保持原样 ——
  改完再记一笔「没人接」，等于谎报做成了
- 没有执行者（比如没装 fetch-router）时，context_care 收得到篡改动作但没有手去做，
  它自己也要产出一条**篡改失效事件**。提醒规则照常有效，篡改规则无效，失效必须看得见
- 校验严格：配置非法**直接拒绝安装**，保持原行为（fetch-router 的风格）
- 核心纯函数，能直接单测

### 可观测与不变量

- 每次命中要记：哪条规则、作用在什么位置、代价多少、结果如何 —— fetch-router 那个右侧栏面板有现成先例（`sidebarRightTabs`，逐请求行 + 顶部计数 + 暂停按钮）
- **变换必须留下记录**：一张**进程内的记录表**，按会话分，照抄 `dsh-tool-websearch` 的 `lib/progress.js`
  （它第一行就写着 `Never touches Session logs`）。**不写 session 事件** —— 查过了：
  `Session.append` 造出来的自定义类型不带 `ignorable`，而会话事件默认 required-on-read，
  不认识的构建会**拒绝整份日志**。第三方插件写自定义事件，
  等于让装了别的插件的人打不开自己的会话。
- 那个不变量是**单向**的：模型看到的必须能从日志重建，**没规定记下来的必须被模型看到**。
  但「logged 但不进上下文」的那些事件（`turn/start`、`request/header`、`assistant/attempt`）
  都是 DSH 核心写的 —— 插件自己的运维信息该留在进程内，要给人看就注册 HTTP 端点。

---

## 三、这条链上已经查清的事实（省得下次重查）

### 前缀缓存为什么是变换的「定义域」

- 改前缀 → 缓存全失效 → 成本和延迟一起翻上去；改尾部几乎免费。所以「改哪里」是钱的问题，不是审美问题。
- **如果没有缓存约束，变换库根本没必要存在** —— 你可以每次重建整个上下文，语义等价。
- `chatseqtrie/toolrouting.go` 是活证据：把 `tools` 换成一个包装工具、真实工具列表挪到尾部动态区段，唯一目的就是让 `tools` **字节级固定**。它的默认约定：`DefaultWrapperToolName = "tool_call"` / `DefaultToolNameField = "tool_name"` / `DefaultToolArgsField = "arguments"`。
- 估算的作用是让变换**可判断**：不知道废掉多少前缀就没法决定值不值，也没法写回归测试（`prefix_cache_exposure_test.go`）。

### chatseqtrie 提供了什么（参考，不照搬）

- `trie.go`：消息序列前缀树。`MatchResult` = `MatchedSession` / `MatchedLen` / `CommonPrefixLen` / `Suffix` / `IsExactMatch` / `IsVariant` / `BranchPoint`。
- `sequencepolicy.go`（**整包最值钱的注释**）：很多 OpenAI-compatible 网关和 chat template **不按 `request.messages` 的表面顺序消费 system**，而是抽取所有 system 合并/前置到有效 prompt 顶部。所以中途插一条 system 时，原始数组看着还是完整前缀，服务端有效 prompt 已在顶部 system 块里分叉 —— **只看原始顺序的 trie 会把巨量缓存失效误判成高命中**。
- `fieldpolicy.go`：哪些字段算「内容」（参与匹配）、哪些算「修饰」（不参与）。JSON Pointer 风格路径 + `*` 通配（`tool_calls/*/arguments`）。
- `openai.go` / `claude.go`：两套 API 格式适配。
- 测试文件名说明了它踩过什么：`silent_failure_test.go` / `robustness_fail_test.go` / `robustness_fail2_test.go` / `deep_bug_test.go` / `semantic_bug_test.go` / `storage_edge_test.go` / `remove_prune_test.go` / `prefix_cache_exposure_test.go` / `concurrency_bench_test.go`。
- **DSH 只需要它最核心那一条洞察**：前缀命中按**字节**算，所以估算也必须按字节算。

### 为什么上下文清洗是必要的（哥哥的诊断）

- 不是「难不难看」的问题，是**污染上下文让模型之后持续倾向于这样思考**的问题。
- 机制：**上下文里的模式就是示例**（few-shot，只不过示例是自己写的）。工具输出脏了是「处理」它；自己的思考脏了是「继承」它 —— 中间没有判断。
- 我们还会**切换模型**，有些模型的思考是总结过的，进上下文未必是好事。
- 把「模型能看到 === 被记录」理解反了，就等于把 DSH 理解成**无法介入上下文的高级抽卡机**。实际上对包括思考内容在内的所有上下文做清洗，**恰恰是一个重要的工程手段**。
- **隐私清洗**是这套机制最重要的用途之一：发送前那一刻是最后的机会，发出去就收不回来。

---

## 四、待定 / 下一步

1. **库名和包名未定**（可能 `@leolee9086/dsh-...`，跟 fetch-router / better-session-query 一个风格）
2. **规则来源的接口形状未定**：提供方 `ctx.provide` 什么、消费者怎么注册「我消费 kind=transform」
3. **面板要不要做**（fetch-router 有先例，可以后加；先靠日志）
4. **实现顺序建议**：匹配与替换（纯函数，可单测）→ 预算与收缩 → 消费者注册 → 两个插件各接一遍
5. 哥哥说「先实现这个规则引擎」—— 已获准开工

---

## 五、关键路径

- 本文件：`D:\dev\dsh-rule-engine\2026-09-22-规则引擎设计.md`
- 酒馆参考：`D:\dev\参考项目\SillyTavern\public\scripts\extensions\regex\`
- fetch-router：`D:\dev\dsh-fetch-router\`（README.md / lib/rules.js / lib/index.js / cordis.patch.yml）
- context_care：`D:\dev\dsh-context-care\`（src/index.js / src/policy.js）
- chatseqtrie：`D:\dev\s-forge\packages\chatseqtrie\`
- 记忆插件：`D:\dev\SAC_search\dsh-better-session-query\`（lib/ + docs/）
- 相关设计文档：
  - `D:\dev\SAC_search\dsh-better-session-query\docs\2026-09-22-第二组伪工具与intent库设计.md`
  - `D:\dev\SAC_search\dsh-better-session-query\docs\2026-09-22-记忆插件三特性与提示规则设计.md`
  - `D:\dev\块编辑器\2026-09-22-路线选择与调研.md`
- profile 补丁层：`C:\Users\al765\.dsh\profiles\web\cordis.patch.yml`
