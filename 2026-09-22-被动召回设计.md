# 被动召回：设计

- 时间：2026-09-22 深夜（哥哥睡前交办）
- 要解决的问题：**我不知道自己不知道要召回以前的记忆**。
  主动召回（`session_blocks_recall`）要求我先意识到「这里可能需要查」，而这恰恰是缺的那一环。
- 做法：索引插件持续看当前上下文，用词法匹配去搜记忆库，得分够高就**主动推**给 context_care，
  由它注入成一条给模型的提醒。
- 通道要通用：以后还有向量召回、别的插件也可能有话要说，所以通道不认识「召回」这件事。

## 数据流

```
用户说了话
   │
   ▼
context_care  agent/pre-step
   │
   ├─ 规则引擎（已有）：静态声明的提醒规则
   │
   └─ 通知通道（新）：问每个注册过的源「这一轮有什么要说的」
          │
          ▼
      better-query 的召回源
          ├─ 从用户输入抽关键词
          ├─ 拿关键词搜记忆库（FTS5 / bm25）
          ├─ 得分 ≥ 阈值才要
          └─ 返回一条通知
          │
          ▼
      context_care 去重 / 冷却 / 注入
```

## 通道：context_care 提供 `contextNotices`

**拉模式**，不是推模式。消费者在每个请求边界问源，源是个纯函数：

```js
ctx.provide('contextNotices', {
  register(name, source),   // source: (ctx) => notice[] | undefined，返回取消注册
})
```

给源看的上下文：

```js
{ agentId, userText, assistantText, signal }
```

通知形状 —— 通道只知道**通知什么**和**什么时候能通知**，别的都不知道：

```js
{
  id,               // 去重与冷却的键（属于「什么时候通知」）
  text,             // 要通知什么
  summary,          // 可选：面板与日志用
  cooldownMinutes,  // 什么时候通知：同一条 id 多久内不重复
  oncePerSurface,   // 什么时候通知：同一段 surface 上只通知一次
}
```

**分数不进通道。** 「得分够不够高」是源自己的判断依据 —— 词法召回看 bm25，
以后的向量召回看余弦距离，通道凭什么要认识这两个东西。源判断完，够格就推，不够格就不推。
通道拿到的东西里没有一个字段跟「召回」有关，所以它能给任何插件用。

为什么是拉不是推：源不需要知道「什么时候该说话」，它只要回答「现在有什么要说的」。
推模式要求源自己监听事件、自己维护时机，多一份状态就多一份不一致。

## 召回源：better-query 提供

1. 用户输入太短就不查（默认 < 8 字）
2. 抽关键词：连续汉字串（≥2 字）+ 英文/数字词（≥3 字），丢掉标点和空白
3. `memory.recall({ keywords, limit })` —— 现成的 FTS5 召回
4. **得分门槛**：SQLite 的 `bm25()` 是负数，越负越相关；取 `-bm25` 当得分，越大越好。
   阈值是配置项，默认先给保守值，跑一段时间按日志调。
5. 命中项要排除「本来就在当前上下文里」的（`recall` 已经做了 `surface === 'current'` 的过滤）

**要改的一处**：`memory.recall` 现在只把 bm25 用于 `ORDER BY rank`，没返回给调用方。
被动召回需要它，所以要把它带进 items。

## 注入文本

```
<被动召回>
你以前记过这些，可能和刚才说的事有关：
- [2026-09-22] 我在长沙
- [2026-09-20] 我喜欢吃白饭
（这是自动召回，不是你主动搜的；要不要用、用哪条，你自己判断。）
</被动召回>
```

## 防刷屏

- `oncePerSurface`：同一段用户输入只推一次
- 冷却：同一条通知（按 id）多久内不重复
- 每轮最多注入一条（把命中合并成一条，不逐条注入）

## 待办顺序

1. 推送三个仓库到 GitHub
2. `memory.recall` 带上 score
3. better-query：`lib/passive-recall.js` + 注册到通道
4. context_care：`src/notice-channel.js` + 在 pre-step 里接上
5. 两边测试
6. 文档
