# 被动召回：实现进度

- 时间：2026-09-22 深夜
- 目标：解决「不知道自己不知道要召回什么」
- 状态：代码写完、测试全绿、**还没推 GitHub、还没真机验证**

## 落了什么

### context_care（3 个新文件 + 2 处改动）

- `src/prompt-text.js`（新）：从消息里取纯文本，规则和通道共用
- `src/notice-channel.js`（新）：通用通知通道
- `src/notice-rules.js`：改用 prompt-text
- `src/index.js`：provide `contextNotices`；pre-step 里 collect + 注入
- `test/notice-channel.test.js`（新，8 个）
- `test/general.test.js`：假 ctx 补 `provide` 和 `logger`

### better-query（1 个新文件 + 2 处改动）

- `lib/passive-recall.js`（新）：召回源
- `lib/index.js`：`ctx.inject(['contextNotices'], ...)` 注册源；导出新符号
- `lib/memory.js`：`recall` 带出 bm25 分数
- `test/passive-recall.test.mjs`（新，9 个）

## 通道的形状（哥哥当场纠过一次）

**通道只知道两件事：要通知什么、什么时候能通知。**

```js
ctx.provide('contextNotices', { register })
// 通知：{ id, text, summary?, cooldownMinutes?, oncePerSurface? }
```

**分数不进通道** —— 那是源自己的判断依据。词法召回看 bm25，以后的向量召回看余弦距离，
通道凭什么认识这两个东西。源判断完，够格就推、不够格就不推。

## 几个判断

- **拉模式，不是推模式**：每个请求边界通道问每个源「这一轮有什么要说的」。
  源不需要知道什么时候该说话，也就不需要自己维护时机状态。
- **一个源出错不挡别的源**：记 warn 跳过。这不是静默，日志里有。
- **关键词抽取不引分词器**：连续汉字串（≥2 字）+ 英文数字词（≥3 字符）。
  多抽几个词只是多几条候选，后面的分数门槛会把关。
- **去重管内容、冷却管时间**：`oncePerSurface` 默认开（同一条 id 在一个会话里只注入一次），
  关掉才看 `cooldownMinutes`。
- **better-query 用 `ctx.inject` 注册**，不用 inject 导出 —— context_care 是可选的，
  没装就不注册，本插件照常工作。

## 测试

- 规则引擎 74
- context_care 71（`test/integration.test.js` 要 DSH_TEST_CHECKOUT，没跑）
- better-query 162

## 推送状态

三个仓库都推完了（2026-09-22 深夜）：

- `leolee9086/dsh-rule-engine` —— 新建仓库，首次推送
- `leolee9086/dsh-context-care` —— `abe70c8` 通用通知通道
- `leolee9086/SAC_search` —— `4422188` 被动召回

## 还没做

1. ~~推 GitHub~~ 已推
2. **真机验证**：要重启 DSH，然后由**人**说话触发（规则和召回都只看真人消息）
3. **调阈值**：`minScore` 默认 1 是保守值，跑一段时间按日志调；
   `minChars` / `limit` / `cooldownMinutes` 同理
