# 交接笔记：规则引擎接入 · 被动召回 · 请求层篡改 · 进度事件调查

- 时间：2026-09-23 上午到中午
- 触发：哥哥连续三轮纠正（「既然知道没做完要我重启做什么」「loss 只跟修改位置有关」
  「你又一次没有做深入调查就得出了结论，看搜索进度事件是怎么推送的」）
- 状态：**代码写完、测试绿、但有一处调查没做完、还没提交推送**

## 一、这次要回答的问题（还没答完）

哥哥的原话：**「你又一次没有做深入调查就得出了结论，看搜索进度事件是怎么推送的」**

上下文：我在给「变换记录」找去处。我先说「DSH 没口子写自定义 session 事件」，
然后改口说「不该写 session 事件，该用进程内状态」（照抄 dsh-tool-websearch 的 progress.js）。
哥哥说这仍然没深入 —— 他让我看**搜索进度事件是怎么推送的**。

### 已经查到的（有据可查）

1. `D:\dev\SAC_search\dsh-tool-websearch\lib\progress.js` 全文 50 行。
   第 1 行注释：`// Process-local progress, owned by one plugin activation. Never touches Session logs.`
   结构：`createProgressStore()` 返回 `{ start(sessionId, callId, signal), get(sessionId, callId), dispose() }`。
   内部 `const sessions = new Map()`，按 sessionId → Map(callId → entry) 两层。
   `entry.progress` 只保留界面要的字段（`latestResults.slice(0, 5)`）。
   `finish()` 时 `signal?.removeEventListener("abort", finish)` 并删掉自己那一格。

2. `lib/index.js`（446 行）里的注册：
   - `const progressStore = createProgressStore();`
     `ctx.effect(() => () => progressStore.dispose(), "dsh-tool-websearch: transient progress");`
   - 常量：`PROGRESS_API = "/api/dsh-websearch/progress"`、`PROXY_API_BASE = "/api/dsh-websearch/proxy"`、
     `SEARCH_API = "/api/dsh-websearch/search"`
   - `ctx.effect(() => webServer.register({ kind: "exact", path: PROGRESS_API, handler: async (req, res) => {...} }), "dsh-tool-websearch: progress api route")`
     端点是 **GET**，取 `url.searchParams.get("sessionId")` 和 `get("callId")`，
     校验 `length > 512` 和 `getAll(...).length !== 1`，然后 `json(res, 200, { progress: progressStore.get(sessionId, callId) ?? null })`
   - `inject = ["tools", "webServer", "connection", "credentials"]`
   - 工具执行时：`const sessionId = exec?.agent?.session?.id; const callId = exec?.callId;`
     `observation = progressStore.start(sessionId, callId, exec?.signal)`，
     `emitProgress = (p) => { try { observation.update(p); } catch { /* Progress observation must never fail the search. */ } }`，
     `finally { observation?.finish(); }`

3. `lib/client.js` 第 281 行左右：`const response = await fetch(PROGRESS_API + "?" + query, {...})`，
   然后 `if (!stopped) setSnapshot({ sessionId, callId, progress: value?.progress })`；
   第 311 行 `const progress = useSearchProgress(props.sessionId, props.callId, !info.done);`
   **→ 客户端是轮询 fetch，不是事件推送。**

4. DSH 核心里的 grep（可能不全，别当成结论）：
   - `packages/core/session/src/types.ts` 搜 `progress` → **No matches found**
   - `packages/core/tools/src` 搜 `progress` → 只有 `ptc.ts` 里一句无关注释

### 还没查的（下次从这里接）

- **`presentCall` 与工具卡的「原始事件」**：DSH 的 `docs/cookbook/adding-a-tool.md` 里有一句
  「Web cards derive from **raw events** and persisted result metadata」。那「raw events」是什么事件、
  谁推、怎么推 —— 很可能是哥哥指的东西。
- `ctx.get('connection')` 这个服务的能力（meta 搜索用它的 `requestRejection(req)`；
  它是不是带推送能力的连接层？）。
- `lib/client.js` 的 `useSearchProgress` 完整实现（约 275-300 行），看它除了轮询还有没有订阅。
- DSH 的 `tool` 定义接口里有没有 progress / 流式输出字段（`packages/core/tools/src` 里 tool 的 interface）。
- `ctx.emit` / client 侧 `ctx.on` 这类跨半部事件通道。

## 二、这一轮已经改完的代码（未提交）

### D:\dev\dsh-context-care\

- **新增 `src\transform-log.js`**：`createTransformLog({ limit = 200 })` →
  `{ record(entry), list(sessionId), stats(sessionId), dispose() }`。
  内部 `bySession` Map，按 sessionId 分桶，每桶超 limit 丢最老的。
  `record` 的字段：`{ sessionId, layer, ruleId, outcome, loss, detail }`；
  sessionId 拿不到时落到 `'__unknown__'`。`stats` 返回 `{ count, applied, blocked, loss }`。
  文件头注释写清了「为什么不写 session 事件」（见下面「踩过的坑」第 3 条）。

- **改 `src\notice-rules.js`**：
  `installNoticeRules(ctx, { plugin, onHit = () => {} })` —— 新增 `onHit` 参数；
  `onRecord` 里第一件事就是 `onHit(record, { sessionId: agent.session?.id })`。

- **改 `src\request-rewrite.js`**：
  - `createEngine()` **不再传 onRecord** —— 引擎回调在 run 内部触发，那时还不知道会话；
    改成从 `engine.run()` 的返回值 `result.records` 里拿，就能一起带上 sessionId。
  - `rewrite({ body, url, scope })` 新增 `scope` 参数；
    `const sessionId = typeof scope?.sessionId === 'string' ? scope.sessionId : undefined`
    然后 `for (const record of result.records) onRecord(record, { sessionId })`。
  - **上一轮修的预算问题**（很重要，别退回去）：
    `const lastSent = new Map()` 按 **host + path** 记住每个接口上一次**真正发出去的** body，
    下次当 `previous` 传给引擎。`targetOf(url)` 用 `new URL(url).host + pathname`。
    没有它 previous 永远是 null、loss 永远是 0、`budget.maxCacheLoss` 形同虚设。

- **改 `src\index.js`**：
  - `import { createTransformLog } from './transform-log.js'`
  - `const transformLog = createTransformLog()`
    `ctx.effect(() => () => transformLog.dispose(), 'dsh-context-care: transform log')`
  - `const recordHit = (layer, record, where) => transformLog.record({ sessionId: where.sessionId, layer, ruleId: record.ruleId, outcome: record.outcome, loss: record.loss, detail: record.detail })`
  - `installNoticeRules(ctx, { plugin: name, onHit: (record, where) => recordHit('notice', record, where) })`
  - 请求层：`onRecord(record, where) { recordHit('request', record, where); ... }`

- 之前几轮还改过（已推送到 GitHub）：`src\prompt-text.js`（新增 `recentToolCalls` / `lastAssistantText`）、
  `src\notice-channel.js`（通用通知通道）、`src\request-rewrite.js`（新增）、
  `src\index.js`（接通道 + 接 requestRewrite）。

### D:\dev\dsh-rule-engine\

- **改 `2026-09-22-规则引擎设计.md` 第 103-104 行**：原来写的是
  「**变换必须留下记录**：一条 **agent 看不到的 session 事件**」，这是错的根源。
  现在改成：一张**进程内的记录表**，按会话分，照抄 `dsh-tool-websearch` 的 `lib/progress.js`；
  并写清为什么不写 session 事件（见下面「踩过的坑」第 3 条）。

- 之前几轮（已推送）：`lib\placement.js` 加 `REQUEST: 'request'`；
  `package.json` 版本升到 **0.1.1**；`test\apply.test.js` 加两个预算语义测试。

### 其他仓库（已推送，供参考）

- `D:\dev\dsh-fetch-router\`：新增 `lib\rewrite.js`（`REWRITE_SERVICE = 'requestRewrite'`，
  `createRewriteRegistry({ warn })` → `{ register(name, rewrite), apply(request), size() }`）；
  `lib\interceptor.js` 加 `rewriteBody()`（只有字符串 body 才改）；
  `lib\index.js` 里 `ctx.provide(REWRITE_SERVICE, { register: rewrites.register })` 并把 `rewrites.apply` 传给 `installFetch`。
- `D:\dev\SAC_search\dsh-better-session-query\`：`lib\notice-rules.js`（「记住」规则）、
  `lib\passive-recall.js`（被动召回源）、`lib\memory.js` 的 `recall` 带出 `score`（bm25）。
- `D:\dev\SAC_search\`（仓库根）：被动召回的改动已提交推送。

## 三、跑过的命令与结果

测试一律逐个文件跑（沙箱里 `node --test` 会 EPERM）：

```powershell
cd D:\dev\dsh-rule-engine
Get-ChildItem test\*.test.js | ForEach-Object { & node $_.FullName }

cd D:\dev\dsh-context-care
Get-ChildItem test\*.test.js | Where-Object { $_.Name -ne 'integration.test.js' } | ForEach-Object { & node $_.FullName }

cd D:\dev\dsh-fetch-router
Get-ChildItem test\*.test.js | ForEach-Object { & node $_.FullName }

cd D:\dev\SAC_search\dsh-better-session-query
Get-ChildItem test\*.test.mjs | ForEach-Object { & node $_.FullName }
```

最后一次性结果：**TOTAL tests=351 failed=0**
（规则引擎 74、context_care 83、fetch-router 30、better-query 162 上下）
加 `transform-log.js` 之后 context_care 仍是 **83 failed=0**（transform-log 自己的测试还没写）。

语法检查用过：`node --check src/xxx.js`（每个改过的文件都过）。

## 四、待办（按优先级）

1. **把「搜索进度事件怎么推送」查完**（哥哥点名要的，见第一节「还没查的」）。
   查清楚之后再决定变换记录要不要跟着改 —— 现在这份 transform-log 可能不是他要的形态。
2. **`transform-log.js` 的测试**（`D:\dev\dsh-context-care\test\transform-log.test.js` 还没建）。
3. **`test\request-rewrite.test.js` 补一个「记录带上会话 id」的用例**
   （onRecord 现在有第二个参数 `{ sessionId }`，现有测试没断言它）。
4. **提交并推送 context_care 和规则引擎**（这次的 transform-log 与文档改动还没提交）。
5. **规则引擎发布 0.1.1**（哥哥跑，我不跑发布）：
   ```powershell
   cd D:\dev\dsh-rule-engine
   npm publish --access public
   ```
   `PLACEMENT.REQUEST` 是新加的，别人装 `^0.1.0` 要能拿到。
6. **重启 DSH** 让被动召回和请求层篡改生效（profile 里全是 `link:` 到本地 checkout，重启即生效）。
   验证要**人**说话触发 —— 规则和召回都只看 `source.kind === 'user'` 的消息。

## 五、踩过的坑与根因

1. **请求层没传 previous，预算形同虚设。**
   现象：`budget.maxCacheLoss` 怎么写都不拦。
   根因：`createRequestRewriter` 跑引擎时只给了 `{ now }`，`previous` 永远 null，
   `cacheLossRatio` 对 null 返回 0。
   修法：按 host+path 记住上一次真正发出去的 body。

2. **改写器没注册消费者，所有规则被判 no-consumer。**
   现象：一条都不执行。
   根因：引擎的 gate 阶段先判「这条规则有没有人接」，没人接的不生效（这是哥哥定的规矩）。
   修法：`engine.registerConsumer({ name: REWRITER_NAME, kinds: ['transform'], handle() {} })`。

3. **「agent 看不到的 session 事件」这个说法是错的。**
   查证：`Session.append(type, data, ...opts)` 的 opts 只在 surface 事件上接受 surface 意图，
   **不产生 `ignorable`**；而 `ignorable` 只在**从磁盘读回来的种子事件**校验里被允许
   （`deepseek-harness\packages\core\session\src\index.ts` 第 225 行：
   `(event['ignorable'] !== undefined && event['ignorable'] !== true)` 被判非法）。
   会话事件默认 required-on-read —— 不认识的构建会**拒绝整份日志**。
   所以第三方插件写自定义类型是有害的，等于让装了别的插件的人打不开自己的会话。
   **但哥哥说这仍然不够深**，让我去看「搜索进度事件是怎么推送的」。

4. **假 ctx 缺 `provide` / `inject` / `logger`。**
   `test\general.test.js` 的假 ctx 加了 `provide`（写进一个 `provided` Map）、`logger`、
   以及 `inject(services, callback)` —— 只有 `provided` 里有的服务才算可用，
   这样「没装 fetch-router 时 requestRewrite 不触发」才测得出来。

5. **测试场景写错会被自己骗。**
   「连续两次请求 loss = 0」我第一次写成期望第一次 loss = 1 —— 错了：
   冷启动没有上一次可比，第一次本来就该是 0。哥哥说的「第一次消耗大」是
   **规则刚生效那次**，它的上一条是没改过的原文。改成用 `let active = false` 控制规则开关才对。

6. **输出循环（模型侧退化）。**
   现象：思考里「（做。）」自我重复 16 次，被 context-care 的 loop-guard 掐断。
   根因：DeepSeek V4 Flash 上的稳定倾向，不是任务的错。
   处理：落盘 + `context_rest` 压缩，压缩后一般恢复。

## 六、关键路径与凭据名

- 交接笔记（本文件）：`D:\dev\dsh-rule-engine\2026-09-23-交接笔记-进度与事件调查.md`
- 规则引擎：`D:\dev\dsh-rule-engine\`（npm 包 `@leolee9086/dsh-rule-engine`，已发 0.1.0，0.1.1 待发）
- 上下文调整插件：`D:\dev\dsh-context-care\`（GitHub `leolee9086/dsh-context-care`）
- 会话索引插件：`D:\dev\SAC_search\dsh-better-session-query\`（在 `leolee9086/SAC_search` 仓库里）
- 请求层插件：`D:\dev\dsh-fetch-router\`（GitHub `leolee9086/dsh-fetch-router`）
- web profile：`C:\Users\al765\.dsh\profiles\web\package.json`（全是 `link:` 到 `D:\dev` 下的 checkout）
- git 凭据：`gh auth setup-git` 已配好（沙箱拦凭据管理器时的绕法）
- npm：哥哥的账号，发布命令给他跑

### 术语（哥哥定的）

- 「上下文调整插件」= `dsh-context-care`
- 「会话索引插件」「会话内记忆插件」= `dsh-better-session-query`
- 「请求篡改服务」= fetch-router 的 `requestRewrite`

### 哥哥这几轮的原则（别弄反）

1. 不做降级回退：行为要 === 意图，意图不清要失败而不是静默篡改。
2. 优先级显式声明（`order` 必填）。
3. 消费者 ≠ 执行者；同一条规则可以多方关心。
4. fetch-router 参与是技术现实（唯一拦 `globalThis.fetch` 的），不是架构角色。
5. 没人接的规则不生效，篡改没执行者时请求保持原样。
6. 通道只知道「通知什么」和「什么时候通知」，分数不进通道。
7. 插件之间不能有依赖，只能通过服务/消息/文件系统/HTTP 端点互相调用。
8. 本地用没关系，说了「发布」就要考虑别人的安装体验。
9. 发布命令给哥哥，他自己跑。
10. **调查要深入，别用 grep 当万灵药，别急着返回结论。**

### 关于进度事件的最后一句

哥哥说「看搜索进度事件是怎么推送的」。到目前为止我确认的只有：
**meta 搜索的进度是进程内 Map + HTTP GET 轮询**（`lib/client.js` 的 `fetch(PROGRESS_API + "?" + query)`）。
如果他认为这里有「推送」，那一定在我还没读的那部分 —— 大概率是
`docs/cookbook/adding-a-tool.md` 里说的「Web cards derive from **raw events**」那条线。
**下次从这里接着查，别再凭 grep 下结论。**
