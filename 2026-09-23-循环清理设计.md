# 循环思考的清理：设计

- 时间：2026-09-23
- 需求（哥哥原话）：「上面这种严重循环的思考模块，直接变换清理，只发送清理之后的干净内容，
  清理范围是 surface，但是要注意前缀缓存稳定性」
- 状态：**已实现**（`src/loop-clean.js` + 接进 pre-step，测试 9 个）
- 场景：2026-09-23 上午我自己输出循环，思考里「（做。）」重复 16 次被掐断。
  现有处理只是**注入一条提醒**让模型自己落盘+压缩 —— 那不够，循环内容还在 surface 上，
  下一轮请求照样把它发出去。

## 现有的两段，缺的是中间那段

| 阶段 | 现成的东西 | 位置 |
|---|---|---|
| 检测（生成中） | `stream-watch` 的 `line-repeat` 触发器，命中就 abort + resume | `src/stream-watch.js` |
| 检测（事后） | `detectLoop(session)` → `{ line, count, total, ratio }` | `src/loop-guard.js:76` |
| 提醒 | `loopNoticeText(hit, aborted)` | `src/loop-guard.js:134` |
| **清理** | **没有** | ← 要做的 |

判据（`loop-guard.js` 顶部的常量）：末尾 80 行窗口、至少 30 行、行长度 ≥ 3、
代码围栏行不参与统计、同一行出现 ≥ 15 次且占比 ≥ 20%。

## 清理在哪一层做

**改这一轮要发出去的消息，不动日志。** 理由：

1. 日志是真相源，模型看到的东西必须能从中重建。清理是「这一次不把它发出去」，
   不是「它没发生过」—— 原文留在日志里，随时查得回来。
2. 从 append 路径看，**在 pre-step 里改 surface 节点是走不通的**：
   `invariant.ts:118-121` 要求 `assistant/message` 处在 open step 内
   （`requireOpenStep`），而 `step/start` 发生在 pre-step **之后**
   （`architecture.md:113`：assembly → `step/start` → `agent/request`）。
   对比 `tool/result`（`invariant.ts:127-136`）—— 它对 surface 替换只要求 open turn。
   所以「替换历史节点」这条路在 pre-step 会被拒。
3. pre-step 的 `decision.messages` 就是这一轮的 surface 投影，改它等价于改发出去的请求，
   而且**不碰任何持久状态**。

## 缓存

- 要清理的是**最后一次助手输出**，它在消息列表的**尾部** —— 只废掉它后面那点前缀。
- 清理**必须是稳定函数**：同样的循环内容必须产出同样的结果。
  第一次请求改了它 → 缓存从前缀断点起失效；第二次请求做同样的清理 → 前缀重新对齐 → 不再花。
  这正是哥哥说的「规则稳定正确实现的话，应该只有第一次生效时消耗较大」。
- 代价用引擎的 `budget.maxCacheLoss` 量（`lib/budget.js` 的 `cacheLossRatio`）。

## 这件事只能打地鼠

哥哥定的调子：**「怎么清理我们只能打地鼠，抓到一个模式清理一个模式，这个是没有办法的事情。」**

循环没有通用判据可写。它是模型侧的退化，换一组词、换一种退化形状就是另一个模式；
判据越通用，误杀正常输出的风险越大（长清单、表格、代码天然重复）。
所以这里不追求「认出所有循环」——那是个不可能的目标，追求它只会写出一个既漏又滥的判据。

能做的是**让抓一个变便宜**：`src/loop-clean.js` 里 `PATTERNS` 是模式表，
加一个模式 = 加一项（一个 `detect` 一个 `clean`），别处都不用动。
每加一个模式，把当时那个真实样本原样抄进注释 —— 样本比描述有用。

`stream-watch.js` 的触发器表是同一种结构。两处早晚该合成一份，现在先各自成表。

## 两个模式（截至 2026-09-23）

| id | 认什么 | 怎么清 | 门槛 |
|---|---|---|---|
| `line-repeat` | **同一行**反复出现 | 从它第一次出现处**截断** | 末尾 80 行窗口内 ≥ 15 次且占比 ≥ 20% |
| `filler-lines` | **一类行**成片出现：行短（≤ 6 字符）且含应答虚字 | 把这些行**逐条删掉**，其余保留 | ≥ 5 行且占比 ≥ 40% |

第二个模式的样本（哥哥给的截图，原样抄在 `PATTERNS` 的注释里）：「做。」只出现 6 次，
够不到 `line-repeat` 的 15 次门槛，但它和「嗯，简洁。」「好。」这类碎句加起来占了窗口六成。
这一类要**按行的性质**认，不是按同一行重复认。

## 代码围栏是硬保护

两个模式都必须先过 `fenceMask(lines)`：代码块内的行（含围栏行本身）一律不参与判定。

理由是**误清代码比漏清退化输出严重得多** —— 前者毁任务材料，后者只是多留一点噪声。
中文注释（`// 做`）、短代码行（`go()`）、示例文本全都够得着 `filler-lines` 的形状，
没有这层挡着就会出事。所以它是任何新模式的**前置条件**，不是可选优化。

## 清理函数

输入：一条 assistant 消息的块数组。输出：清干净的块数组。

- 只在 `reasoning` / `text` 块上做。
- 对文本按行扫，**从末尾往前**找连续重复段：一旦某行在尾部窗口内重复超过阈值，
  从它**第一次出现**的位置起截断，剩下的丢掉。
- 保底：清理后不能把整块清空到没有 —— 如果截断点在第 0 行，就保留第一段，
  并在末尾加一行说明「（这里原本有一段循环输出，已清理）」。
  这句话要写进替换物里，因为模型需要知道「这里被处理过」，否则它看到的是一段突然中断的思考。

## 记录

每次清理写一条 `context-care/transform` log-only 事件（`src/transform-log.js`），
字段照旧：`{ at, layer: 'loop', ruleId, outcome, loss, detail }`。
全链路能不能安全加载已经查过（六处证据写在 `transform-log.js` 头注释里）。

## 还没定

- 清理后要不要顺带**压缩**一次（提醒文案里现在写的是「先落盘再压缩」）。
  两件事的触发条件不同，先分开。
- 阈值要不要可配。现在复用 `loop-guard.js` 的常量。

## 实现记录

- `D:\dev\dsh-context-care\src\loop-clean.js`（新）：
  `cleanTail(text)` / `cleanMessage(message)` / `cleanMessages(messages)`。
  阈值复刻 `loop-guard.js`：末尾 80 行窗口、行长度 ≥ 3、围栏行不参与、重复 ≥ 15 次且占比 ≥ 20%。
  截断点是循环行在窗口里**第一次出现**的位置；截断点落在第 0 行时保留前四分之一，
  免得整块凭空消失。
- `src\index.js` 的 pre-step：检测到循环就先 `cleanMessages(messages)`，
  再写一条 `context-care/transform` log-only 事件（layer: 'loop'），最后照旧提醒。
  清理与提醒是两件事：清理负责「别再发出去」，提醒负责「你该落盘了」。
- 测试：`test\loop-clean.test.js` 9 个，其中一条是跟 `detectLoop` 的串联，
  一条是**幂等**（清过的再清一次不再变）—— 缓存要的就是这个。
