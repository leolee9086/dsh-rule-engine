# 请求层篡改：实现进度

- 时间：2026-09-23 上午
- 场合：哥哥说「when.produced 和 when.idle 拿不到只能是因为你写错了，fetch-router 就在本地，你应该修改实现」
- 状态：三边都改完、测试全绿、已推 GitHub、**规则引擎待发布 0.1.1**

## 两件事

### 一、助手输出与工具调用历史（我之前没去拿）

数据一直都在。会话事件在 `session.surface.nodes` 上，用 `session.eventAt(node)` 取回来，
信封上有 `time`（Unix 毫秒），`tool/call` 的 data 是 `{ turn, step, callId, name, arguments }`。

`src/prompt-text.js` 补了两个：

- `recentToolCalls(session, { limit })` —— 从后往前扫 surface，收集 `tool/call`，
  按时间正序返回。`when.idle` 找「最近一次调用」用它。
- `lastAssistantText(session)` —— 找最后一条 `assistant/message`，取纯文本。`when.produced` 用它。

`notice-rules.js` 跑引擎时把这两个填进 ctx，不再只给 `userText`。

### 二、请求层篡改

**为什么篡改要在请求层做**：pre-step 只能改「这一轮要发出去的消息」，
而组装好的请求体里还有系统提示词、工具定义等等 —— 那些只有请求层看得见。

**规则引擎加了第六个位置**：`PLACEMENT.REQUEST = 'request'`，指「组装好的整段请求体」。
所以规则可以用 `placement: ['request']` 声明「这条规则在请求层生效」。

**fetch-router**（`lib/rewrite.js` 新文件）：

```js
ctx.provide('requestRewrite', { register(name, rewrite) })
// rewrite: ({ url, method, body, scope }) => 改后的 body | undefined
```

- 只有字符串 body 才改。流、FormData、URLSearchParams 不碰 ——
  读它们要么得整段缓冲、要么得改语义，不是这一层该替调用方做的决定。
- 一个改写器出错不挡别的，记 warn 跳过。
- 改过就记一笔 `outcome: 'rewritten'`，面板上看得到。

**context_care**（`src/request-rewrite.js` 新文件）：

`ctx.inject(['requestRewrite'], ...)` 登记一个改写器，跑 `placement: ['request']` 的 transform 规则。
规则来源跟提醒规则共用 `memoryNoticeRules` —— 规则里用 placement 区分它该在哪一层生效。

## 预算的真问题（哥哥指出后修的）

哥哥说「loss 只跟修改位置有关，规则稳定正确实现的话应该只有第一次生效时消耗较大，
如果不是这样，说明你的实现本身有巨大问题」。

loss 的算法本身是对的 —— 我补了两个测试验证：

- `同一条规则连续两次：只有第一次废缓存` —— 第二次 loss 确实是 0
- `loss 只跟修改位置有关：越靠前越贵` —— 改第 1 个字符废掉整条，改最后 1 个只废 1 字节

**但请求层根本没传 previous。** `createRequestRewriter` 跑引擎时只给了 `{ now }`，
于是 `previous` 永远是 null、loss 永远是 0、`budget.maxCacheLoss` 形同虚设 ——
规则再费缓存也拦不住。这才是「巨大问题」在的地方。

修法：改写器按 **host + path** 记住每个接口上一次真正发出去的 body，下次拿它当 previous。
这也正是「规则稳定时只有第一次费缓存」的来源 —— 上一次发出去的已经是改过的文本，前缀自然对得上。

补了两个用例：规则刚生效那一次 loss = 1，之后 loss = 0；不同 host 各记各的上一次。

## 踩到的一个坑

改写器一开始没注册消费者，结果所有规则被判成 `no-consumer`、一条都不执行。

原因是引擎的 gate 阶段先判「这条规则有没有人接」——没人接的规则不生效
（这是哥哥定的：篡改没有执行者时请求保持原样）。所以改写器必须自己注册成消费者，
它**就是**篡改动作的接住者。接住之后不用做什么：改后的文本就是 `engine.run` 的返回值，
那一环的语义是「这条规则有主」。

## 测试

- 规则引擎 74
- context_care 81（新增 `prompt-text.test.js`、`request-rewrite.test.js`）
- fetch-router 30（新增 `rewrite.test.js`）
- better-query 162

## 还没做

1. **规则引擎发布 0.1.1** —— `PLACEMENT.REQUEST` 是新加的，别人装 `^0.1.0` 要能拿到它。
2. **请求层的记录进不了 session 事件，原因是 DSH 没给这个口子。** 查证：
   `Session.append(type, data, ...opts)` 的 opts 只在 surface 事件上接受 surface 意图，
   不产生 `ignorable`；而 `ignorable` 只在**从磁盘读回来**的种子事件校验里被允许
   （`packages/core/session/src/index.ts` 第 225 行）。
   会话事件默认 required-on-read：不认识的构建会**拒绝整份日志**。
   所以第三方插件写自定义类型是有害的，不能做。
   现在记录走 `onRecord` 回调，context_care 接的是 logger。
   要变成 session 事件，得先给 DSH 加口子 —— 那是改 DSH，等哥哥决定。
3. **真机验证**：要重启 DSH，然后由人说话触发。
4. **缓存预算在请求层的实际效果**：`budget.maxCacheLoss` 已经生效（引擎会算 loss），
   但请求体是整块的，loss 会很大 —— 真用起来需要先想清楚规则该怎么写。
