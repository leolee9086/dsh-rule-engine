# 规则引擎实现进度

- 时间：2026-09-22
- 位置：D:\dev\dsh-rule-engine\
- 意图：按设计文档实现 DSH 专用规则引擎库，**先不接入任何插件**
- 结果：库已经能跑，六个测试文件全绿

## 落了什么

```
lib/placement.js   作用位置与作用域常量
lib/regex.js       正则编译、替换、宏替换（搬运自酒馆正则扩展）
lib/rules.js       规则校验与规范化（非法直接抛错）
lib/match.js       触发条件匹配（全部 AND）
lib/budget.js      前缀缓存的字节级估算与预算收缩
lib/apply.js       把规则作用在一段文本上（纯函数）
lib/engine.js      规则来源、消费者注册、冷却与去重、命中日志
lib/index.js       汇总导出
test/*.test.js     六个测试文件
README.md          接口形状与用法
```

## 哥哥当场纠的一处

我说「优先级 = scope + order + 声明顺序」时，哥哥说**要显式声明优先级**。
他是对的：规则可能来自静态配置，也可能来自运行时由别的插件提供，合并之后
「谁先声明」这件事根本不存在 —— 拿它当优先级是假的。
现在 `order` 缺失直接抛错，同 scope 同 order 按 id 定序（只为让顺序确定）。

## 实现里几个要记住的判断

- **`budget.maxCacheLoss` 是「这条规则自己造成的增量损失」**，不是整段文本的总损失。
  多条规则叠加时，增量累加才是总代价。
- **`selectWithinBudget` 的 required 是给隐私清洗留的**：无条件接受、代价照算，
  超预算由调用方决定拒绝发送；一般超预算走批准。
- **`idle` 在从没调用过时返回 false**，否则会话开头所有提醒会一起响。
- **`transform` 找不到消费者时仍然会改文本**，只是没人用；记录里标 `no-consumer`。
  这是有意的：applyRules 是纯函数，不知道谁在消费。

## 踩到的坑

- PowerShell 双引号里 `$1` 会被当变量吃掉，所以**测试必须写成文件跑**，
  不能用 `node -e` 传代码 —— 我第一轮就被这个骗了一次，以为替换语法坏了。
- TS 模板字符串里写 JS 正则，反斜杠要双写；漏写一次就是 `SyntaxError`（match.js 的
  `isRegexLiteral` 中过一次）。
- 酒馆的 `regexFromString` 在 flag 非法时会**把整个输入当 pattern**，不返回 null。
  所以 `/(a)/z` 编译出来是匹配 `/a/z` 的正则，不是报错。这是酒馆的降级行为，保留了。

## 下一步

1. 接 fetch-router：改 body + 规则来源运行时提供（它现在不写会话日志、不发 session 事件）。
2. 接 context_care：取 notify，走 `agent.inject(notice(...))`。
3. 面板（可选）：照 fetch-router 的 `sidebarRightTabs`。
4. 规则来源的接口形状：现在只是一个返回规则数组的函数，卸载与预设绑定还没想。

## 验收

七个测试文件全绿。沙箱里 `node --test` 会 EPERM，逐个跑 `node test/xxx.test.js`。
test/integration.test.js 把各层拼起来跑：预算收缩、思考清洗、隐私清洗、两个消费者、冷却。

## 哥哥 2026-09-22 的第二轮纠正：不做降级回退

原话：**不要做太多降级回退，尽可能让行为 === 意图，如果意图不清应该失败而不是静默篡改它。**

查了一遍，第一版里到处都是降级，全部改掉了：

- 正则编译不出来时 `runRegexScript` 直接返回原串、`matchRule` 返回 `{ok:false}` —— 静默失效，
  现在一律抛错；NONE 模式的正则在规范化阶段就编译一次，坏的当场拒绝安装。
- `regexFromString` 照搬了酒馆「flag 非法就把整个输入当 pattern」的降级，
  于是 `/a/z` 会悄悄变成匹配 `a/z` 的正则 —— 现在判为编译失败。
- `replaceString` 省略时兜底成空串（= 删除匹配）—— 现在省略抛错，空串才是删除。
- `surface.text ?? ''`、`String(text ?? '')` 这类兜底全部去掉，非字符串抛错。
- 布尔字段用 `=== true` / `!== false` 静默转换，现在给了就必须是布尔。
- `id`、`order`、transform 的 `budget.maxCacheLoss` 改成必填。
  `budget` 尤其要紧：默认 0 会让规则在真实缓存场景下永远静默跳过。

顺手发现一个真 bug：`matchRule` 判命中时直接用 `when.findRegex`，没走宏替换，
而 `runRegexScript` 走了 —— RAW / ESCAPED 模式下两边看到的正则不一样。
现在两边都走 `resolveFindRegex`。

## 待办：context_care 的「记住」规则（哥哥 2026-09-22 交办）

用户说了「记住」、并且明确要求记住某样东西时，要调用会话内记忆工具。
**规则本身由索引插件（dsh-better-session-query）声明**，不是写死在 context_care 里 ——
正好对上 `engine.provideRules(name, fn)` 这个运行时规则来源的口子。
接 context_care 的时候一起做。

## 哥哥 2026-09-22 的第三轮澄清：消费者与执行者不是一回事

我上一轮理解错了两处，都是往收窄的方向错的：

- 把 `action.by` 当成了「这条规则归哪个执行者」，于是想让它必填、想把消费者收窄成管理者。
  实际上 `by` 只是「这条规则交给谁处理」，跟谁去执行改动无关。
- 把 context_care 的**职责范围**当成了**管辖范围**。它负责「通知模型」和「篡改上下文」两类效果，
  不代表所有规则命中都得先经它手。同一条规则可以多方关心：
  循环输出时 context_care 提醒模型，另一个插件提醒用户。

「上下文提醒和修正一定经过 context_care」靠的是**统一入口 + 它订阅**：
上下文相关的意图都声明在规则引擎里，它订阅这些规则，所以一定看得到全貌。
不是靠引擎把规则都塞给它。

关于 fetch-router，哥哥的原话是：**它必须参与进来纯粹是因为只有它一个插件在介入请求层**。
所以它是「当前唯一能碰请求层的插件」，不是架构上的执行者角色 ——
将来换一个插件能做这件事，就换它做，引擎这边什么都不用改。

实现上撤回上一轮的三个提议：`action.by` 保持可选（不写就按 kind 广播），
引擎里不引入「管理者」概念，`registerConsumer` 注册的就是关心规则的人。

## 哥哥 2026-09-22 的第四轮：没人执行的篡改等于没做

原话：**没有 fetch router 的时候，context_care 的提醒规则是有效的，但是篡改规则是无效的，只产出一个篡改失效事件。**

这条改动落在了引擎里。原来 transform 规则命中就先改文本，然后才看有没有消费者接；
没人接就记一笔 `no-consumer`，但返回值里的 `text` 已经是改过的了 ——
调用方拿到一个「改好了」的文本，其实没人执行。这就是行为与意图不符。

现在 `planDelivery` 在 gate 阶段就判「有没有人接」：

- 没人接 → 规则不执行，文本保持原样，只留下一条 `no-consumer` 失效记录
- 有人接 → 照常执行，把动作交给它

提醒类走同一条路：没人接的提醒不会凭空出现。

还有一层在消费者那边，引擎管不了：装了 context_care 但没装 fetch-router 时，
它**收得到**篡改动作，只是没有手去执行 —— 这时候要由它自己产出篡改失效事件。
引擎这边能保证的是「没人接就不做」，保证不了「接了就能做」。
